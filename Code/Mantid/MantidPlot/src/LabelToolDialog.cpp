/* #include "LabelToolDialog.h"

// Constructor for the LabelToolDialog.

LabelToolDialog::LabelToolDialog(QWidget * parent = 0, Qt::WindowFlags f = 0 ) : QDialog (parent, f = 0)
{
   m_uiform.setupUi(this);
}

*/