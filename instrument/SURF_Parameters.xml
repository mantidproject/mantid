<?xml version="1.0" encoding="UTF-8" ?>
<parameter-file instrument = "SURF" valid-from = "2012-01-31T00:00:00">
<component-link name = "SURF">
  <parameter name="MonitorIntegralMin">
    <value val="4.0"/> 
  </parameter>

  <parameter name="MonitorIntegralMax">
    <value val="6.0"/>
  </parameter>

  <parameter name="MonitorBackgroundMin">
    <value val="7.6"/>
  </parameter>

  <parameter name="MonitorBackgroundMax">
    <value val="8.5"/>
  </parameter>

  <parameter name="PointDetectorStart">
    <value val="2"/>
  </parameter>

  <parameter name="PointDetectorStop">
    <value val="2"/>
  </parameter>

  <parameter name="MultiDetectorStart">
    <value val="1"/>
  </parameter>

  <parameter name="I0MonitorIndex">
    <value val="1"/>
  </parameter>

  <!-- In the old Reflectometry UI, LambdaMin's value has 1 added to it.
       In the new Reflectometry UI, this adjustment is NOT made. -->
  <parameter name="LambdaMin">
    <value val="0.5"/>
  </parameter>

  <!-- In the old Reflectometry UI, LambdaMax's value has 2 subtracted from it.
       In the new Reflectometry UI, this adjustment is NOT made. -->
  <parameter name="LambdaMax">
    <value val="7.0"/>
  </parameter>

  <!-- parameters for efficiency correction -->
  <parameter name="correction" type="string">
    <value val="exponential"/>
  </parameter>

  <parameter name="C0">
    <value val="36.5688"/>
  </parameter>

  <parameter name="C1">
    <value val="0.188676"/>
  </parameter>

</component-link>
</parameter-file>
