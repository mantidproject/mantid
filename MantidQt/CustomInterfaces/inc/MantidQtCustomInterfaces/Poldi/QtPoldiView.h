#ifndef MANTIDQTCUSTOMINTERFACES_POLDIVIEW_H_
#define MANTIDQTCUSTOMINTERFACES_POLDIVIEW_H_

#include "MantidQtAPI/UserSubWindow.h"
#include "MantidQtCustomInterfaces/Poldi/IPoldiPresenter.h"
#include "MantidQtCustomInterfaces/Poldi/IPoldiView.h"
#include "ui_QtPoldiView.h"
#include <memory>

namespace MantidQt {
namespace CustomInterfaces {

class QtPoldiView : public MantidQt::API::UserSubWindow, public IPoldiView {
  Q_OBJECT

public:
  /// The name of the interface as registered into the factory
  static std::string name() { return "Poldi Reduction"; }
  // This interface's categories.
  static QString categoryInfo() { return "Poldi"; }

public:
  /// Default Constructor
  QtPoldiView(QWidget *parent = 0);
  /// Print info
  void giveUserInfo(std::string prompt, std::string title) override;

private:
  /// Initialize the layout
  void initLayout() override;

private slots:
  void loadDemoTriggered();

private:
  /// The form generated by Qt Designer
  Ui::PoldiWidget m_uiForm;
  /// The presenter
  std::unique_ptr<IPoldiPresenter> m_presenter;
};
}
}

#endif // MANTIDQTCUSTOMINTERFACES_POLDIVIEW_H_
