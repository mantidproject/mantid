- Absorption and scattering corrections now account for the sample's orientation instead of silently ignoring it. :ref:`algm-MonteCarloAbsorption`, :ref:`algm-PaalmanPingsAbsorptionCorrection`, :ref:`algm-DiscusMultipleScatteringCorrection`, :ref:`algm-MultipleScatteringCorrection`, :ref:`algm-XrayAbsorptionCorrection`, the single-path mode of :ref:`algm-AddAbsorptionWeightedPathLengths`, and the algorithms that take their shape from the sample rather than building their own - :ref:`algm-AbsorptionCorrection`, :ref:`algm-CuboidGaugeVolumeAbsorption` and :ref:`algm-CylinderAbsorption` where no cylinder dimensions are given - all traced lab-frame rays through a sample shape that was still in its own frame, so a workspace oriented with :ref:`algm-SetGoniometer` alone was corrected as though the sample had never been turned. The gauge volume, the beam and the detectors are all described in the lab frame, so the sample is now moved there too. Results are unchanged for a workspace with no goniometer, and for one whose shape was baked by :ref:`algm-CopySample` - both already described the sample in the lab frame. Only the previously incorrect case changes, and the two ways of orienting a sample now give the same answer.
- This matters most alongside the change to :ref:`algm-RotateSampleShape`, which no longer folds the run's goniometer into the shape. That rotation used to leave the shape in the lab frame as a side effect, which is the frame these algorithms assumed; correcting them together means a ``SetGoniometer`` then ``RotateSampleShape`` workflow gives the same answer as before rather than losing the goniometer rotation.
- Correcting from the container or the sample environment is deliberately unchanged: neither is ever goniometer-rotated anywhere in Mantid, which is the same reason :ref:`algm-SetSample` declines to bake the run's goniometer into a sample that has an environment. :ref:`algm-MayersSampleCorrection` is also unchanged, because it sizes a cylinder from the shape's bounding box and rotating the shape first would only inflate that box.
- The Monte Carlo mode of :ref:`algm-AddAbsorptionWeightedPathLengths` cannot yet do this, because its interaction volume and beam profile are built once while the goniometer rotation varies from peak to peak. It now warns when that case arises rather than returning numbers computed with the sample in the wrong place.
