- :ref:`algm-Rebin` no longer intermittently fails with a spurious *"This binning is expected to create ... bins"* memory error when the machine is busy. The safeguard against oversized rebins now compares the estimated output size against the machine's total physical memory (with some headroom) instead of the momentarily-available memory, so a valid rebin gives the same result regardless of what else is running.
