- In :ref:`algm-CreatePoleFigureTableWorkspace`, two new options have been added to the inputs. ``SpectraWorkspace`` allows the user to provide a name for a new workspace which contains only the spectra corresponding to rows in ``OutputWorkspace``. ``IncludeSpectrumInfo`` is a flag which adds information columns to the final ``OutputWorkspace`` about where each spectra has come from.
- ``texture_helper.py`` has been created to hold stand alone texture functionality which is independent of specific instrument workflows
- Pole Figure plots can now be created from ``texture_helper`` function with a debug flag which allows interactive annotation of points in the pole figure by holding "a" key on the keyboard and clicking
