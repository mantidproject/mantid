- The algorithm `LoadEventNexus` when loading single banks on the TOPAZ or MANDI instruments
    will no longer appear to hang, and will no longer throw an error on completion.
    The remaining instrument will have only the single bank loaded, and no other components.
