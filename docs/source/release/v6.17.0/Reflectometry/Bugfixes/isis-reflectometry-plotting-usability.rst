- Improved the ISIS Reflectometry Plotting tab so muted workspace rows are clearer, detector map overplotting is disabled, repeated alignment/spin-asymmetry/detector-map plots reuse generated workspaces, generated plotting workspaces no longer overwrite each other for ``++`` and ``--`` spin-state suffixes, stitched outputs are muted for alignment and detector map plots, and tiled detector maps use the selected y-axis label on every tile.
