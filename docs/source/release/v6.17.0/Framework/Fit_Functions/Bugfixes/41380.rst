- Defined fitting parameters in ``Parameters.xml`` files no longer erroneously overwrite if they have the same name but for different functions e.g. ``Bk2BkExpConvPV:Gamma`` and ``IkedaCarpenterPV:Gamma``.
